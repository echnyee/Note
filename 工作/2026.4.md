# 1
#性能优化
https://github.com/jlfwong/speedscope#usage

# 20
#Golang
## 类型概念
## 1) 基础类型（concrete type / 具体类型）

像这些都是**具体类型**（不是接口）：
- 基础：`int`, `string`, `float64`, `bool`
- 复合：`struct`, `[]T`, `map[K]V`, `*T`
- 你自定义的命名类型：`type MyInt int`

具体类型的变量里放的是“具体值”，例如：
Go
```
var x int = 3
```
---

## 2) 接口类型（interface type）

接口类型定义了一组“方法集合”（method set），例如：

Go
```
type error interface { Error() string }
type Stringer interface { String() string }
```
以及空接口：

Go
```
interface{} // 或 any
```
- `interface{}` 没有任何方法要求，所以**任何具体类型**都实现它。

---

## 3) 静态类型（static type）

**静态类型**就是“变量在编译期声明的类型”。

例如：

Go
```
var i interface{} = "hello"
```
这里 `i` 的静态类型是 `interface{}`（编译器看到 i 就把它当接口来处理）。

再如：
Go

```
var err error = ErrNegativeSqrt(-2)
```

这里 `err` 的静态类型是 `error`。

静态类型决定了：
- 你能对变量调用哪些方法/做哪些操作（编译期检查）
- 赋值/传参是否类型匹配

---

## 4) 动态类型（dynamic type）与动态值（dynamic value）

当一个变量的静态类型是 **接口 时，它在运行时会装着某个具体值。此时接口值可以理解为一对：
- **动态类型**：里面装的具体类型是什么（例如 `string`、`*MyError`）
- **动态值**：那个具体值本身（例如 `"hello"`、`&MyError{...}`）
例子：
Go
```
var i interface{} = "hello"
```
- 静态类型：`interface{}`
- 动态类型：`string`
- 动态值：`"hello"`

你可以用：
Go
```
fmt.Printf("%T\n", i) // 打印动态类型：string
fmt.Printf("%v\n", i) // 打印动态值："hello"
```
## 初始化速记
- **slice**：能用 `var s []T; append` 就别急着 `make`；能估计长度就 `make([]T, 0, n)`
- **map**：要写就 `make` 或字面量；`var m map[...]...` 只适合延迟分配/只读
- **二维 slice**：不规则直接逐行 `make`；规则矩阵也逐行；性能敏感用连续内存
- **struct**：字段初始化用 `T{}` 或 `&T{}`，默认配置用零值
- **泛型**：`Type[Concrete]` 实例化；泛型函数多依赖推导；要比较就 `comparable`
- **io**：优先用 `io.Copy` 等组合函数；写 wrapper 按固定三步走

# 21
#Golang 
## tutorial
https://go.dev/doc/tutorial/

# 23
#Golang 
## GC
几个简单的结论
1. GoGC很多时候是cpu和内存的平衡，给的内存多，gc频率低，cpu友好，给的内存少，内存友好，cpu消耗高
2. mark-sweep，从root开始walkthrough，也叫scan，找到不可达的内存，进行可分配标记，是非移动型的GC，有些GC在gc过程中会移动内存，让内存都放在一起。go不会
3. GC成本
	1. 主要成本（dominant）：**marking & scanning（标记与扫描）**，并且和 **live heap 大小、指针密度/结构**强相关：指针越多、结构越“难并行”(比如链表/树深)，单位字节扫描成本越高。
	2. 固定成本：每轮初始化/准备之类的小开销
	3. sweep 成本被文档认为相对小，在 Go 当前实现里通常远小于 mark/scan
4. 通过root节点开始scan，就不会有引用计数的循环引用问题，例如最经典的A<->B互相引用，在他们释放之后，引用计数仍然>0，而在root scan的流程中，如果A和B释放之后（仅存在互相的引用），那么从root那边scan下来，就会断开了，会被认为是不可达的。

# 26
#AI #ClaudeCode
## CC大致工作流程
一些粗浅的理解，大致来说用户输入文本之后，CC会根据当前环境，所用语言，预设的系统级prompt，组装成一个完整的prompt，然后和用户输入文本一起发给大模型，让其决策，后续需要进行哪些操作，有了操作列表之后，会在本地再进行相应的权限检查，工具调用，得到后续的结果，再继续调用大模型，直到达到最后的结果。入口再QueueEngine.submitMessage这里。
另外每一个会话session，都只会启动一个QueueEngine，这个对象会保存上下文信息，权限拒绝信息，已发现的skill信息，token使用情况等等，是保障cc能连续工作的一个基础对象。
![[Pasted image 20260426112738.png|697]]![[Pasted image 20260426112820.png]]

``` ts
function getSimpleIntroSection(
  outputStyleConfig: OutputStyleConfig | null,
): string {
  // eslint-disable-next-line custom-rules/prompt-spacing
  return `
You are an interactive agent that helps users ${outputStyleConfig !== null ? 'according to your "Output Style" below, which describes how you should respond to user queries.' : 'with software engineering tasks.'} Use the instructions below and the tools available to you to assist the user.

${CYBER_RISK_INSTRUCTION}

IMPORTANT: You must NEVER generate or guess URLs for the user unless you are confident that the URLs are for helping the user with programming. You may use URLs provided by the user in their messages or local files.`

}
```
这里就是默认的系统级提示词模版，会根据当前环境，上下文，语言，轮次进行拼装
后续调用工具的时候也会有工具级别的提示词，可以提示模型这些工具可以怎么用，参数可以怎么填。

### 压缩
每次发送消息给LLM之前也会有一些压缩，例如工具返回的大量输出，需要进行一些裁剪和压缩，只取部分最有价值的最相关的内容。
![[Pasted image 20260427094741.png]]
### MCP Server
目前理解，cc启动时会去配置的MCP server进行一次拉取，获取一下目前拥有的mcp能力，这些能力是可以不断增加扩展的。比方说现在有一些浏览器的mcp接口，例如说Chrome，那拿到这些能力之后可以交给模型判断是否可以去使用。
### Skills
cc会在对应的路径里面去加载，mcp server也可能会暴露skills，这样配置好对应的mcp server之后就可以大家共享skills了。发给模型时是Skill的摘要，名字，做哪些事情。模型会判断是否要使用某些Skill。可以认为skill就是一些可复用的流程经验或者团队的风格。使用skill的时候也会判断是直接主agent直接执行，还是开一个子agent单独执行。我理解要看这个skill对上下文的依赖程度和对于token的使用情况，对上下文依赖越少（例如一些概要信息就可以了），对token使用越多（可能会污染主agent，或者消耗主agent下会话上下文）的时候，越需要新开子agent

